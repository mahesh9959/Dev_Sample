pipeline{
    agent any
    stages{
        stage("Welcome"){
            steps{
                echo "Welcome back to Jenkins declarative pipeline from GIT integration"
            }
        }
    }
}
